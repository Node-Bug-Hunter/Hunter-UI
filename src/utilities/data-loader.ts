
export class DataLoader {
    static pullProfileData() {
        // 
    }

    static pullResources() {
        // 
    }

    static pullServerData(name: string) {
        // 
    }

    static pullTemplate(tname: string) {
        // ToDo: fetch from cdn only if not available in localStorage
        // ToDo: Save to localStorage once fetched to save load time & bandwidth
    }
}
